apiVersion: v1
kind: Service
metadata:
  name: order-delivery-management
  labels:
    app: order-delivery-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: order-delivery-management